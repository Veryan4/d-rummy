import { css } from "lit";

export const styles = css`
  .lobby {
    font-family: var(--font-family);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 80vh;
  }
`;
