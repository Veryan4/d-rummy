import { css } from "lit";

export const textFieldStyles = css`
  mwc-textfield {
    --mdc-theme-primary: var(--theme-color);
    --mdc-text-field-fill-color: var(--input-fill);
    --mdc-text-field-ink-color: var(--primary-color);
    --mdc-text-field-label-ink-color: var(--primary-color);
  }
`;
